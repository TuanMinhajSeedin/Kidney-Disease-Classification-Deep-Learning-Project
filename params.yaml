AUGMENTATION: True
IMAGE_SIZE: [224,224,3]
BATCH_SIZE: 16
INCLUDE_TOP: False   #Removing fully connected layers and taking only convolution layers
EPOCHS: 2
CLASSES: 2
WEIGHTS: imagenet   #Because of pre trained model
LEARNING_RATE: 0.03